import React from 'react';
import Logo from '../../assets/logo.png'
import './styles.css';

const Header = () => {
    return (
        <header>
            <img src={Logo} />
        </header>
    )
};

export default Header;
